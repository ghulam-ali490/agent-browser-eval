# Upstream Vercel repo cloned for reference only; not part of this eval
# Anchored to repo root so it does NOT also match results/agent-browser/
/agent-browser/

node_modules/
*.log

# Playwright MCP auto-dumps
.playwright-mcp/
